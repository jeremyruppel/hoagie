Usage: <%=: package.name | green | dim %> <%-: '<command>' | cyan %> <%=: '[--version] [--help]' | gray %>

<% var max = function(arr){ return Math.max.apply(null, arr); } -%>
<% var len = max(commands.map(function(str){ return str.length; })) -%>
Where <%-: '<command>' | cyan %> is one of the following:
<% commands.filter(help).forEach(function(command) { -%>
   <%=: command | ljust:len | cyan %>  <%= help(command) %>
<% }) -%>

See '<%=: package.name | green %> help <%-: '<command>' | cyan %>' for help with a specific subcommand.
