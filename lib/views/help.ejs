Usage: <%=: package.name | blue %> <%-: '<command>' | white %> <%=: '[--version] [--help]' | gray %>

<% var max = function(arr){ return Math.max.apply(null, arr); } -%>
<% var len = max(commands.map(function(str){ return str.length; })) -%>
Where <%-: '<command>' | white %> is one of the following:
<% commands.forEach(function(command) { -%>
   <%=: command | ljust:len | white %>  <%= package.subcommands[command] %>
<% }) -%>

See '<%=: package.name | blue %> help <%-: '<command>' | white %>' for help with a specific subcommand.
