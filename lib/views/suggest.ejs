<%=: package.name | blue %>: '<%= command %>' is not a <%= package.name %> command. See '<%= package.name %> --help'.
<% if (suggestions.length > 1) { %>
Did you mean one of these?
<% suggestions.forEach(function(cmd) { -%>
   <%=: cmd | white %>
<% }) -%>
<% } else if (suggestions.length === 1) { %>
Did you mean this?
   <%=: suggestions[0] | white %>
<% } -%>
